import { IconPencil } from '@shared/icons';
import styles from './IconCheck.module.scss';

export const TableIconCheck = () => {
  return <IconPencil className={styles.wrapper} width={20} height={20} />;
};
