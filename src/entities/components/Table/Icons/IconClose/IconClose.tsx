import { IconClose } from '@shared/icons';
import styles from './IconClose.module.scss';

export const TableIconClose = () => {
  return <IconClose className={styles.wrapper} width={20} height={20} />;
};
